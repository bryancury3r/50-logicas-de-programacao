const base = parseInt(prompt("digite a base:"));

const altura = parseInt(prompt("digite a altura:"));

const retangulo = base * altura;

document.write(`A área do retângulo corresponde a: ${retangulo}`);

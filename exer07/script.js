const nota1 = parseInt(prompt("digite primeira nota:"));

const nota2 = parseInt(prompt("digite sugunda nota:"));

const nota3 = parseInt(prompt("digite terceira nota:"));

const media = nota1 * 2 + nota2 * 3 + (nota3 * 5) / 10;

document.write(`sua media é: ${media}`);
